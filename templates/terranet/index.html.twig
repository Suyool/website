<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>TerraNet</title>
    {% block stylesheets %}
        {% for file in encore_entry_css_files('terraNetstyle') %}
            <link rel="stylesheet" href="{{ asset(file|substr(1)) }}">
        {% endfor %}
    {% endblock %}
</head>

<body class="TerraNet">
<div id="TerraNet" data-data="{{ parameters|json_encode }}"></div>
</body>

{% block javascripts %}
    {% for file in encore_entry_js_files('TerraNet') %}
        <script src="{{ asset(file|substr(1)) }}"></script>
    {% endfor %}
{% endblock %}
</html>
