<div class="alfa-mastercard">
    <div class="row">
        <div class="col-lg-7 col-12 order-lg-1 order-2">
            <div class="card-desc">
                <div class="title">{{ bookKeeping.title|raw }}</div>
                <div class="text sub-title">{{ bookKeeping.subTitle }}</div>
                {% for checkText in bookKeeping.checks %}
                    <div class="text check">
                        <img src="{{ asset('build/images/check.svg') }}" alt="">
                        <p class="desktop">{{ checkText }}</p>
                    </div>
                {% endfor %}
            </div>
        </div>
        <div class="card-img-desk col-lg-5 order-lg-2 order-1">
            <img src="{{ asset('/build/images/digital_corporate/'~liveOverwlImagePath) }}" alt="">
        </div>
    </div>
</div>